export interface Recipe {
    title: string;
    ingredients: Array<string>;
    conributor: string;
    picture: string;
}

export interface Preparation {
    caption: string;
    number: number;
    text: string;
}
